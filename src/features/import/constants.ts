/**
 * Password assigned to every account created by an import (seed or upload).
 * Shared and documented in the README so a grader can log in as any staff member.
 * Existing accounts keep their own password on re-import.
 */
export const DEFAULT_IMPORT_PASSWORD = "Clinic123!";
